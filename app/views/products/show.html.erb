<% if  user_signed_in? and current_user.own?(@product) %>

<%= link_to "編輯商品", edit_product_path(@product), class: 'ink-btn' %>
<%= link_to "刪除商品", product_path(@product), class: 'ink-btnink-btn-danger', 
                                                data: { turbo_method: 'delete',
                                                        turbo_confirm: '確定刪除嗎？'}%>

<% end %>                                                        

<h1 class="text-2xl"><%= @product.title %></h1>
<h3>售價：<%= @product.price %></h3>
<p><%= simple_format(@product.description) %></p>

<section>
  <h3 class="text-xl">留言</h3>

<%= form_with model: [ @product, @comment ], data: { turbo: false } do |f| %>
  
  <%= f.label :content %>
  <%= f.text_area :content %>
  <div>
    <%= f.submit '新增留言', class: 'ink-btn'%>
  </div>
<% end %>

  <ul>
  <% @comments.each do |comment| %>
    <li><%= comment.content %></li>
  <% end %>
  </ul>
</section>

